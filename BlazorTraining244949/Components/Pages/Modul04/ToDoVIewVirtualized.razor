@page "/todoV"
@inject ToDoVM VM
<h3>ToDoView</h3>

<div class="container">

    <div class="row">
        <div class="col">
            <ul class="list-group">
                <li class="list-group-item active">todo</li>
@* 
                @foreach (var item in VM.ToDo.todos)
                {
                    <li class="list-group-item  @(item.completed?"list-group-item-dark":"")">
                        @item.todo
                    </li>
                } *@
                <Virtualize  Items="VM.ToDo.todos" Context="item" OverscanCount="5" ItemSize="30" >
                    <ItemContent>
                        <li class="list-group-item  @(item.completed?"list-group-item-dark":"")">
                            @item.todo
                        </li>
                    </ItemContent>
                      <EmptyContent>
                           <li class="list-group-item">no data</li>
                      </EmptyContent>
                   
                </Virtualize>
            </ul>
        </div>

    </div>
</div>

@code {

}
